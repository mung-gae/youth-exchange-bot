export function migrate(db) {
    db.exec(`
CREATE TABLE IF NOT EXISTS schema_migrations(id INTEGER PRIMARY KEY, applied_at TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS guild_settings(guild_id TEXT PRIMARY KEY, admin_role_id TEXT, event_channel_id TEXT, timezone TEXT NOT NULL DEFAULT 'Asia/Seoul');
CREATE TABLE IF NOT EXISTS events(event_id INTEGER PRIMARY KEY AUTOINCREMENT,guild_id TEXT NOT NULL,name TEXT NOT NULL,status TEXT NOT NULL,current_round INTEGER NOT NULL DEFAULT 1,starting_fund INTEGER NOT NULL DEFAULT 15000 CHECK(starting_fund>=1),participant_role_id TEXT,selected_season TEXT,sale_start_at TEXT,sale_end_at TEXT,created_at TEXT NOT NULL,ended_at TEXT,channel_id TEXT, CHECK(status IN ('SETTING','BUYING','BUYING_CLOSED','BUYOUT_SETTING','BUYOUT_PUBLISHED','SEASON_SELECTED','SELLING','SELLING_CLOSED','SETTLED','ENDED')));
CREATE UNIQUE INDEX IF NOT EXISTS ux_active_event ON events(guild_id) WHERE status!='ENDED';
CREATE TABLE IF NOT EXISTS participants(event_id INTEGER NOT NULL,user_id TEXT NOT NULL,balance INTEGER NOT NULL DEFAULT 0 CHECK(balance>=0),starting_fund_paid INTEGER NOT NULL DEFAULT 0 CHECK(starting_fund_paid IN(0,1)),status TEXT NOT NULL DEFAULT 'ACTIVE',registered_at TEXT NOT NULL,PRIMARY KEY(event_id,user_id),FOREIGN KEY(event_id) REFERENCES events(event_id));
CREATE TABLE IF NOT EXISTS items(item_id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT NOT NULL UNIQUE,season TEXT NOT NULL,price INTEGER NOT NULL CHECK(price>=0),price_tier TEXT NOT NULL,is_public INTEGER NOT NULL DEFAULT 0 CHECK(is_public IN(0,1)));
CREATE TABLE IF NOT EXISTS inventories(event_id INTEGER NOT NULL,user_id TEXT NOT NULL,item_id INTEGER NOT NULL,quantity INTEGER NOT NULL DEFAULT 0 CHECK(quantity>=0),PRIMARY KEY(event_id,user_id,item_id),FOREIGN KEY(event_id,user_id) REFERENCES participants(event_id,user_id),FOREIGN KEY(item_id) REFERENCES items(item_id));
CREATE TABLE IF NOT EXISTS round_buyouts(event_id INTEGER NOT NULL,round INTEGER NOT NULL,item_id INTEGER NOT NULL,buyout_amount INTEGER NOT NULL CHECK(buyout_amount>=0),is_public INTEGER NOT NULL DEFAULT 0,source TEXT NOT NULL DEFAULT 'ITEM',PRIMARY KEY(event_id,round,item_id),FOREIGN KEY(event_id) REFERENCES events(event_id),FOREIGN KEY(item_id) REFERENCES items(item_id));
CREATE TABLE IF NOT EXISTS sale_requests(event_id INTEGER NOT NULL,round INTEGER NOT NULL,user_id TEXT NOT NULL,item_id INTEGER NOT NULL,quantity INTEGER NOT NULL CHECK(quantity>=0),status TEXT NOT NULL,settled INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,PRIMARY KEY(event_id,round,user_id,item_id),FOREIGN KEY(event_id,user_id) REFERENCES participants(event_id,user_id),FOREIGN KEY(item_id) REFERENCES items(item_id));
CREATE UNIQUE INDEX IF NOT EXISTS ux_active_sale_request ON sale_requests(event_id,round,user_id,item_id) WHERE status='ACTIVE';
CREATE TABLE IF NOT EXISTS settlements(event_id INTEGER NOT NULL,round INTEGER NOT NULL,item_id INTEGER NOT NULL,total_quantity INTEGER NOT NULL,unit_price INTEGER NOT NULL,total_paid INTEGER NOT NULL,unused_buyout INTEGER NOT NULL,settled_at TEXT NOT NULL,PRIMARY KEY(event_id,round,item_id),FOREIGN KEY(event_id) REFERENCES events(event_id));
CREATE TABLE IF NOT EXISTS transactions(transaction_id INTEGER PRIMARY KEY AUTOINCREMENT,event_id INTEGER NOT NULL,round INTEGER NOT NULL,user_id TEXT NOT NULL,item_id INTEGER,type TEXT NOT NULL,quantity INTEGER NOT NULL DEFAULT 0,amount INTEGER NOT NULL DEFAULT 0,reason TEXT,created_at TEXT NOT NULL,operation_key TEXT,FOREIGN KEY(event_id) REFERENCES events(event_id));
CREATE UNIQUE INDEX IF NOT EXISTS ux_transaction_operation ON transactions(operation_key) WHERE operation_key IS NOT NULL;
CREATE TABLE IF NOT EXISTS admin_audit_logs(log_id INTEGER PRIMARY KEY AUTOINCREMENT,event_id INTEGER,round INTEGER,admin_id TEXT NOT NULL,action TEXT NOT NULL,target_user_id TEXT,before_value TEXT,after_value TEXT,reason TEXT,created_at TEXT NOT NULL,operation_key TEXT);
CREATE UNIQUE INDEX IF NOT EXISTS ux_audit_operation ON admin_audit_logs(operation_key) WHERE operation_key IS NOT NULL;`);
    console.log('[청춘거래소] 데이터베이스 마이그레이션 완료');
}
